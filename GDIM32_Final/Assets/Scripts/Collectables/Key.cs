using System.Xml.Linq;
using Unity.Jobs;
using UnityEngine;

public class Key : Collectable
{
    // Start is called before the first frame update
    [SerializeField] private string keyColor; //temp variable until key logic is more defined

    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }

}